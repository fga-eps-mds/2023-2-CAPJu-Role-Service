version: "3.3"

services:
  role:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: role
    command:
      [
        "npm",
        "run",
        "start",
      ]
    restart: always
    volumes:
      - ./:/app
    ports:
      - 8084:8084
    networks:
      - 2023-2-capju-config_capju
    env_file:
      - .env

networks:
  2023-2-capju-config_capju:
    external: true
